language: elixir
elixir:
    - 1.3.4
services:
    - postgresql
install:
    - cd ./delicious_elixir
    - mix local.hex --force
    - mix deps.get
before_script:
    - psql -c 'create database postgres_test;' -U postgres
script:
    - mix test
