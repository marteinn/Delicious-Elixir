<h2>Edit admin link</h2>

<%= render "form.html", changeset: @changeset,
                        action: admin_link_path(@conn, :update, @link) %>

<%= link "Back", to: admin_link_path(@conn, :index) %>
